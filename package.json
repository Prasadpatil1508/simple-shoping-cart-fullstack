{
    "name": "simple-shopping-cart-fullstack",
    "version": "1.0.0",
    "description": "Full-stack shopping cart application",
    "scripts": {
        "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
        "backend": "cd backend && npm run dev",
        "frontend": "cd frontend && ng serve --port 3001",
        "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install --legacy-peer-deps",
        "build-all": "cd backend && npm run build && cd ../frontend && ng build"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}